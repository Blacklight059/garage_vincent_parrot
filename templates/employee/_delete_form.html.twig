<form method="post" action="{{ path('app_employee_delete', {'id': employee.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cette prestation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee.id) }}">
    <button class="btn btn-lg m-3" id="btn-connexion">Supprimer</button>
</form>
